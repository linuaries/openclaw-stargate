# 🔄 任务路由规则

## 路由决策流程

```
用户请求 (QQ/Discord)
    │
    ▼
[SGC Command] 任务分析
    │
    ├──► GPU计算任务 ─────────► SGC本地 (图像/声音/Embedding)
    │
    ├──► 代码/文档任务 ───────► SGC本地 (GLM-5 + 72线程)
    │
    ├──► 国际资源访问 ────────► Atlantis Expedition
    │
    ├──► SGC繁忙 ─────────────► Atlantis (备用计算)
    │
    └──► 离线/移动 ───────────► SG-2 Recon
```

---

## 详细路由规则

### 1. GPU计算任务 → SGC Command

**触发条件:**
- `task_type == 'image_processing'`
- `task_type == 'audio_processing'`
- `task_type == 'embedding'`

**路由目标:** `self` (SGC本地)

**示例:**
```
用户: "用AI画一只猫"
用户: "把这段语音转文字"
用户: "生成这些文档的向量表示"
```

**理由:** SGC有RTX 3060 GPU，适合这些任务

---

### 2. 代码生成 → SGC Command

**触发条件:**
- `task_type == 'code'`
- `contains('写代码', '实现', '函数', '编程')`

**路由目标:** `self` (SGC本地)

**使用的模型:** `zai/glm-5` + 72线程编译

**示例:**
```
用户: "用Python写快速排序"
用户: "编译这个项目"
```

---

### 3. 国际资源访问 → Atlantis

**触发条件:**
- `resource_location == 'international'`
- `domain in ['openai.com', 'huggingface.co', 'github.com']`
- `task_type == 'web_fetch_international'`

**路由目标:** `atlantis-expedition`

**示例:**
```
用户: "获取OpenAI博客最新文章"
用户: "从HuggingFace下载模型"
```

**理由:** Atlantis在新加坡，访问国际资源更顺畅

---

### 4. 长文档分析 → SGC Command

**触发条件:**
- `doc_size > 10000 tokens`
- `task_type == 'rag'`

**路由目标:** `self` (SGC本地)

**理由:** SGC有62GB RAM，适合处理大文档

---

### 5. SGC繁忙 → Atlantis (备用)

**触发条件:**
- `sgc_load > 80%`
- `task_priority == 'low'`

**路由目标:** `atlantis-expedition`

**理由:** Atlantis作为备用计算节点

---

### 6. 离线/移动 → SG-2

**触发条件:**
- `offline == true`
- `source_node == 'sg2-recon'`
- `source_location == 'mobile'`

**路由目标:** `sg2-recon`

**理由:** SG-2可在离线时独立工作

---

## 路由优先级

```yaml
routing_priority:
  1: gpu_compute          # GPU任务最高 (必须在SGC)
  2: code_generation      # 代码生成 (SGC有编译优势)
  3: long_document        # 大文档 (SGC有内存优势)
  4: international_access # 国际访问 (Atlantis网络优势)
  5: backup_compute       # 备用计算 (SGC忙时)
  6: simple_chat          # 简单问答 (默认SGC)
  7: offline_mode         # 离线任务 (SG-2)
```

---

## 故障转移

### SGC Command故障

```
SGC Command 离线
    │
    └──► Atlantis接管
         ├──► 轻量任务: 正常处理
         └──► GPU任务: 等待SGC恢复或使用API降级
```

### Atlantis故障

```
Atlantis离线
    │
    └──► SGC Command接管所有任务
         └──► 国际访问任务: 延迟增加但可继续
```

---

## 监控命令

```bash
# 查看路由统计
tail -f ~/.openclaw/logs/sgc-command.log | grep routing

# 查看各节点负载
openclaw fleet status --metrics

# 测试特定路由
curl http://100.64.0.1:18789/route-test \
  -d '{"task_type": "image_processing", "content": "test"}'
```

---

*"Chevron 7 encoded. Destination: Atlantis."* 🛸🌊
